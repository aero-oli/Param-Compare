{
  "name": "ardupilot-param-compare",
  "version": "1.0.0",
  "private": false,
  "description": "Static ArduPilot parameter comparison tool with an OpenAI-powered parameter assistant.",
  "author": "ArduPilot Param Compare",
  "main": "electron-main.js",
  "scripts": {
    "check": "node --check app.js && node --check desktop-key-store.js && node --check electron-main.js && node --check server.js",
    "desktop": "electron .",
    "dist": "electron-builder --publish never",
    "dist:linux": "electron-builder --linux --publish never",
    "dist:mac:signed": "electron-builder --mac --publish never -c.forceCodeSigning=true",
    "dist:win:signed": "electron-builder --win --publish never -c.forceCodeSigning=true",
    "pack": "electron-builder --dir -c.win.signAndEditExecutable=false",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "marked": "^18.0.3"
  },
  "devDependencies": {
    "electron": "^38.4.0",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.ardupilot.paramcompare",
    "productName": "ArduPilot Param Compare",
    "asar": true,
    "files": [
      "assets/**/*",
      "app.js",
      "desktop-key-store.js",
      "electron-main.js",
      "index.html",
      "server.js",
      "styles.css",
      "node_modules/**/*",
      "package.json"
    ],
    "directories": {
      "output": "release"
    },
    "win": {
      "icon": "build/icon.ico",
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "mac": {
      "icon": "build/icon.icns",
      "target": [
        "dmg",
        "zip"
      ],
      "category": "public.app-category.utilities",
      "hardenedRuntime": true,
      "gatekeeperAssess": false
    },
    "linux": {
      "icon": "build/icon.png",
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Utility"
    }
  }
}
